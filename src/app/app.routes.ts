import { Routes } from '@angular/router';
import { PageComponent as LandingPageComponent} from './landing/page/page.component';

export const routes: Routes = [
    {
        component: LandingPageComponent,
        path: "",
    },
];
